# This file was generated by Ansible. You can change it to your
# hearts content, but if this file is deleted, it will be
# recreated with defaults (which is eth0 and dhcp)

network:
  ethernets:
    eth0:
      dhcp4: true
      dhcp-identifier: mac
      accept-ra: true
      optional: true
# Example of a fixed IP address configuration:
#   Note: Leave 'optional: true' on all the interfaces, as it stops
#   network-config from hanging if an interface is down or broken.
#
#    eth0:
#      optional: true
#      dhcp4: false
#      accept-ra: true
#      addresses: [ 10.65.11.x/24 ]
#      routes:
#        - to: default
#          via: 10.65.11.1
#      nameservers:
#        addresses: [ 1.1.1.1, 8.8.8.8 ]

  version: 2
