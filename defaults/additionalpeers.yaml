# Add additional peers in this file - config/additionalpeers.yaml
#   If you are editing defaults/additionalpeers.yaml, that will
#   not work!
#
# Note that there are default settings in 'pjpeerdefaults.yaml', too
# that set context, provider, and inboundcontext if not provided
# here.
---
pjpeers:
  - name: Peer1
    host: 10.10.10.10
    authuser:
    authpass:
    aicontext:
    aiprovider:
    inboundcontext:
    disabled: true
